package effective.java.item6.ch1;

/**
 * 불필요한 객체 생성을 피하라.
 * 특히, 문자열에대해서 이야기를 강조한다.
 * auto boxing -> 기본타입(int)을 그것과 상응되는 기본타입(Interger)로 상호변환해주는기술
 * 기본타입과 박싱된 기본타입을 섞어서 사용하면 변환하는 과정에서 불필요한 객체가 생성될수있다.
 *
 */
public class Strings {
}
